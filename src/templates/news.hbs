{{#each this}}
<li>
    <a href="{{url}}" target="_blank" rel="noopener noreferrer">
        <article>
            <img src="{{urlToImage}}" width="480" alt="">
            <h2>{{title}}</h2>
            <p>Posted by: {{author}}</p>
            <p>{{description}}</p>
        </article>
    </a>
</li>
{{/each}}